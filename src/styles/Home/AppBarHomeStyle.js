import React, { Component } from 'react'
import { StyleSheet } from 'react-native'

import { mainColor } from '../../const/color'

export const AppBarHomeStyle = StyleSheet.create({
    container: {
        backgroundColor: mainColor,
        width: '100%',
        paddingVertical: 15,
    }
})
